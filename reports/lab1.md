荣誉准则 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

无

此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

无

我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

实现功能总结 完成新的系统调用sys_trace,依据传入的参数完成

1、读取/写入当前程序部分内存的效果

2、查询当前用户程序特定系统调用次数的功能

简答作业 **1、**运行chapter2中的三个bad程序后，会出现PageFault的trap报错/IllegalInstruction错误导致任务被强制关闭/Utf8Error的转化错误

我使用的sbi版本在通过qemu-system-riscv64 -machine virt -nographic -bios default查询后显示为OpenSBI v1.0

2、

①刚进入_restore时，由于此时还没有把内核栈的值还原回寄存器，此时sp应该指向的是内核栈的栈顶

_restore的两种使用情景：此处的trap特权级切换中寄存器的恢复；类似的任务切换的_switch函数寄存器的保存与恢复

②处理了sstatus/sepc/sscratch三个特殊的寄存器

sstatus 控制CPU的特权级，可以控制当前的访问权限

sepc 保存用户程序的返回地址

sscratch 保存用户栈指针，用于内核栈和用户帐的切换

3、

x2是sp，已经经过单独处理

x4是tp，用于存储线程局部状态，在第二章中提到它一般不会被使用

4、

交换了两个寄存器中的值，此时sp指向用户栈顶，sscratch指向内核栈顶

5、

sret指令回到用户态：

使用 sstatus.SPP 来决定恢复到哪个特权级（user/supervisor）。 使用 sepc 作为返回地址跳回用户程序。 根据 sstatus 恢复中断使能（SPIE -> SIE）。 6、

交换了两个寄存器中的值，此时sp指向内核栈顶，sscratch指向用户栈顶

7、

从 U 态进入 S 态是在_alltraps最后调用的trap_handler中调用的syscall系统调用函数中的ecall指令来实现的